name: app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  syncfusion_localizations: ^18.1.36
  cupertino_icons: ^1.0.2
  shared_preferences:
  flutter_svg: ^0.19.3
  syncfusion_flutter_calendar: ^18.4.48
  date_time_picker: ^2.0.0
  google_place: ^0.4.0
  flutter_dotenv: ^3.1.0
  firebase_core: ^1.0.2
  firebase_auth: ^1.1.2
  google_sign_in: ^5.0.1
  provider: ^5.0.0
  cloud_firestore: ^1.0.7
  google_maps_flutter: ^2.0.3
  location: ^4.1.1
  permission_handler: ^7.1.0
  flutter_google_places: ^0.3.0
  geolocator: ^7.0.3
  http: ^0.13.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  # firebase_core: #firebase 추가
  firebase_analytics:
  firebase_database:
  firebase_messaging:

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/svg/